export default function MetaMaskButton() {
    return <button className="bg-gradient-to-r from-slate-100 to-slate-300 text-[#111427] rounded-full px-4 py-2 font-bold text-sm hover:scale-95 transition">METAMASK</button>
}
